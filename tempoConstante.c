#include <stdio.h>


int main() {
    int array[] = {1, 2, 3, 4, 5};
    int operacoes = 0;
    // Operação constante (imprimir o primeiro elemento)
    printf("Elemento 0: %d\n", array[0]);
    operacoes++;
    printf("Operações realizadas: %d\n", operacoes);
    return 0;
}
